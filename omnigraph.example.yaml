version: 1

servers:
  prod:
    endpoint: http://127.0.0.1:8080

graphs:
  local:
    storage: ./repo.omni
  dev:
    server: prod
    bearer_token_env: OMNIGRAPH_BEARER_TOKEN

defaults:
  graph: local
  branch: main

query:
  roots:
    - queries
    - .
